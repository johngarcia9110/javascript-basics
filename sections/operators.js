// Basic Operators

// Assignment: = 
// Math: +, - , * , /
// Compound Assignment: +=, -=, *=, /=
// Increment/Decrement: ++, --
// Property Access: . ( example: console.log() )
// Equality: == (loose-equals), === (strict-equals), !== (strict not-equals)
// Comparison: <, >, <=, >=
// Logical: && (and), || (or)


var a = 0;

a = a + 2; // this is the same as..

console.log(a); //2

var a = 0;

a += 2; //..this shortcute operator.

console.log(a);//2

// Both versions of the expression do the same thing, which one you should use is subjective.

